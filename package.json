{
  "name": "direct-voyage",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node scratch/validate_e2e_routes.cjs && node scratch/validate_full_fidelity.cjs",
    "check": "npm test && npm run build"
  },
  "devDependencies": {
    "vite": "^6.2.0"
  }
}
